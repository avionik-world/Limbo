groups:
  admin:
    - limboserver.stop
    - limboserver.kick
    - limboserver.say
    - limboserver.gamemode
    - limboserver.dependencies
    - limboserver.plugins
  default:
    - limboserver.spawn
    - limboserver.chat
    - limboserver.version

players:
  MrManHD:
    - admin